---
status: ACTIVE
---
# CEO Plan: AgentOS v2 — AI-Native IDE and Terminal
Generated by /plan-ceo-review on 2026-03-28
Updated: 2026-03-28 (blockers resolved, scope updated)
Mode: SELECTIVE EXPANSION
Branch: main | Repo: joyboy257/agentos (https://github.com/joyboy257/agentos)

## Vision

### 10x Check
AgentOS becomes the environment where anyone — not just developers — can harness multi-agent AI systems. The IDE metaphor earns its keep when the alternative is watching a ChatGPT tab and hoping it does the right thing. A marketing manager can assemble a "Google Ads optimization team" — a research agent, a copywriter agent, a budget allocator agent — on a visual canvas, connect them with handoffs, and watch them work. No code. No terminal. No tmux grids. Every agent leaves a trace; every handoff is visible; every failure is caught by the Orchestrator. The secret sauce is the UI/UX taste and ease-of-use that makes non-technical people feel empowered, not overwhelmed. The view mode (Kanban, node workflow, etc.) adapts to their mental model. The compounding moat is runtime lock-in: once agents run in AgentOS, their workflows, memory, and policies live there.

### Dream State Mapping
```
  CURRENT STATE                           THIS PLAN                            12-MONTH IDEAL
  ───────────────────────────────────     ───────────────────────────────────  ─────────────────────────────────
  tmux grids + scroll                    Electron app: canvas + runtime       AgentOS as standard env for AI ops
  Claude Code (solo, terminal)           Multi-agent canvas + Orchestrator    Non-technical users building agent teams
  Agents: invisible, ephemeral           Agents: visible, persistent          Agents: observable, recoverable
  No orchestration                       Watch-and-learn policy engine         Predictive failure prevention
  No revenue model                        Freemium: solo free, pro paid        Per-seat or usage-based revenue
  ChatGPT users (solo, chat-only)       Visual multi-agent canvas             Anyone can assemble an agent team visually
```

---

## Six Forcing Questions

### Q1: Demand Reality

**What it says:** "Karpathy described managing agent fleets with tmux grids. Every competitor has 1-2 pillars. No tool combines all three." Status: Early/Analogical.

**The CEO challenge:** "Karpathy" is a single anecdote. "Every competitor" is a category argument, not proof. "The gap is visible in how power users cope" — name one specific power user who told you they're coping this way. Analogical demand is the weakest form. What's the specific behavior that would prove this demand is real?

**What would change this:** If a user says "I literally have 4 tmux panes open right now and I hate it" — that's demand. If the exit criterion for v0.1 is "5 indie devs say they'd switch," that's a real behavioral test, not a survey. Is that the bar?

**Assessment:** The demand case is plausible but unvalidated. The 5-indie-dev exit criterion is the right test. The risk is that 5 devs saying "I'd switch" and 5 devs actually switching after 2 weeks of rough edges are different things.

---

### Q2: Status Quo

**What it says:** "tmux grids + custom scripts." The workaround is fragile, manual, non-visual, isolated.

**The CEO challenge:** Is "I use tmux" actually painful enough to switch? Most power users who use tmux actually *like* tmux. The pain isn't the tmux grid — it's when something fails and you don't see it. Does the status quo have a name and a face, or is it a generic category?

**Assessment:** The tmux grid pain is specific and real for multi-agent users. This is the strongest part of the demand case because it's describing an actual workflow, not a hypothetical.

---

### Q3: Desperate Specificity

**What it says:** "Indie dev / solo builder (3-5 personal agents)."

**The CEO challenge:** Not a name. Not a company. "Indie dev" is a segment, not a person. What's the actual person who needs this today? What are they building? What would make them pay $20/month vs. using tmux for free?

**The narrowest wedge framing is right.** "See all my agents on a canvas with live state" is a concrete value prop. But who is the first 10 users? YC-backed founders with 3+ agents? AI hobbyists building side projects? Something else?

**Assessment:** The wedge is correctly narrowed (indie dev, not enterprise). But the specific persona — what they're building, what their agent workflows look like, what they tried before — needs to be sharper before fundraising or scaling.

---

### Q4: Narrowest Wedge

**What it says:** "Visual agent canvas — see all agents on a canvas with live state, connect them, see capability edges emerge."

**The CEO challenge:** Is THIS the thing worth $0? Worth $20/month? Or is the narrower wedge just "I want to run my Claude Code session in a tab and see its state"? The canvas with handoff edges is the most visually impressive feature — and the hardest to explain in a single sentence. If someone asks "what does it do?" and the answer is "it's like VS Code for agents," that's a $100M positioning budget away from clarity.

**Try this instead:** "It runs your AI agents and shows you what they're thinking — every tool call, every handoff, every failure — in one window." Not "canvas." Not "IDE."

**Assessment:** The canvas is the right differentiator, but the value articulation for a non-technical buyer ("it shows agents on a canvas") doesn't survive first contact. The Orchestrator is actually the more compelling wedge for team leads (watch all my agents) — but that's Phase 3, not Phase 1.

---

### Q5: Observation & Surprise

**What it says:** No user research conducted yet.

**The CEO challenge:** Have you watched anyone use a tmux grid for agent management? Have you seen someone try to explain their multi-agent setup to a colleague? What surprised you about what agents actually do when they're running?

**Assessment:** No observation has been done yet. The plan correctly identifies this as "analogies" not evidence. The first real evidence is the 5-dev exit criterion in v0.1.

---

### Q6: Future-Fit

**What it says:** "AI agents are autonomous processes." As agent frameworks proliferate (Claude Code, Cursor, Copilot Workspace), the need for agent infrastructure grows.

**The CEO challenge:** "AI agents need an IDE" assumes agents stay in development/tooling context. But what if the future is agents running in production infrastructure (agents as services, not desktop tools)? Then AgentOS as a desktop IDE becomes a development-time tool, not a runtime control plane. Is the desktop the right anchor, or should this be server-side?

**Assessment:** The desktop-as-anchor is both a strength (no cloud dependency, full process control) and a risk (if agents move to server-side infrastructure, the TAM shrinks). The plan correctly identifies that Claude Code-style terminal-first has won in the market — but that product is solo and session-based. The gap AgentOS fills (multi-agent, visual, persistent) is real — but it's a bet that multi-agent setups become common enough to need orchestration.

---

## Blocking Decisions — RESOLVED

| Blocker | Severity | Resolution |
|---------|----------|------------|
| Revenue model | HIGH | **RESOLVED:** Freemium: Solo free / Pro $20/seat/month. Solo devs get canvas + runtime free. Pro tier (1-5 seats) pays $20/seat/month. Team features (shared workspaces, fleet overview) are v2 — pricing is set ahead of that launch. |
| MCP integration complexity | HIGH | **RESOLVED:** Phase 0 validates MCP using the **filesystem MCP server** as the reference implementation (stdio stdio handshake, server lifecycle, tool schema). Week 1: MCP SDK validation. Week 1.5: custom JSON-RPC fallback if Week 1 fails. Escalate by end of Week 2 if both fail. |
| v0.1 success metric is wrong | HIGH | **RESOLVED:** Kill condition: "agent completes a task from handoff start to finish without manual intervention." Canvas engagement (≥3 agents, ≥1 edge) is a leading indicator only. |
| Persona is category-level, not name-level | MEDIUM | **RESOLVED:** Target persona = non-technical power users: marketing managers, ops leads, small business owners who use ChatGPT but have never built a multi-agent workflow. They can reason about "a team that handles my Google Ads" without knowing what a terminal is. Secondary: indie hackers building AI products (Phase 1 onwards). |

---

## Glossary

| Term | Definition |
|------|------------|
| **Capability edge** | A labeled connection between two agents on the canvas representing a known handoff or data flow. Example: "Research Pipeline" = researcher agent → verification agent. Displayed as a labeled directed edge. |
| **Live state** | Real-time status of an agent captured via IPC events (spawn, stdout data, tool call, error, exit) flowing from the agent process to the renderer via MessagePort. Not a polling mechanism — push-based via Electron IPC. |
| **Watch-and-learn** | Orchestrator mode in Phase 3 where it observes agent event patterns (tool call durations, error rates, handoff delays) for 24-72 hours before surfacing policy suggestions. Evolves into predictive mode as patterns accumulate. The Orchestrator dashboard shows observed patterns with confidence scores. Users accept or dismiss suggestions. Accepted policies are stored in SQLite and evaluated against live agent events. |

---

## Vision Assessment

**Is this the right thing to build?** Yes — the gap is real and visible. The tmux grid workaround is the clearest demand signal. The multi-agent canvas is the right core positioning.

**Is this the right time?** Yes — Claude Code has validated the "agent as terminal-native process" mental model. The market is warm. The tooling (Electron, React Flow, xterm.js, MCP) is mature. No one has combined all three pillars yet.

**Is this the right person to build it?** Partially — the solo engineer has a massive scope (Electron + React + MCP + PTY + SQLite + canvas). The Phase 0 runtime sprint is the right first proof-of-concept. If it takes longer than 2 weeks, the scope risk is real.

**Primary positioning:** "Build your own AI agent team — visually, without code." Supporting: "Connect agents on a canvas, watch them work, let the Orchestrator catch failures before they cascade."

**Mission (locked in):** Democratize multi-agent AI for the uninitiated. The masses use ChatGPT. A small slice runs multi-agent systems with tmux grids. AgentOS brings multi-agent orchestration to the marketing manager who has never written a line of code but manages a team that could be 10x more productive with AI agents running their workflows.

**Views are the UI layer.** The right view is not yet known — Kanban, node workflows, and others each serve different mental models. Phase 1 should prototype at least two view modes and test with users. The Runtime + Orchestrator underneath is what makes any view compelling. This is the secret sauce — not which view wins, but that the underlying system makes all views come alive.

**Phase Timeline:**
| Phase | Estimated Duration | Notes |
|-------|------------------|-------|
| Phase 0: Runtime validation | 2 weeks | MCP validation sprint |
| Phase 1: Canvas MVP | 8-12 weeks | React Flow + xterm.js display |
| Phase 2: Full runtime | 8-10 weeks | UtilityProcess + MCP + SQLite |
| Phase 3: Orchestrator | 6-8 weeks | Policy engine + watch-and-learn |
| **Total** | **24-32 weeks** | ~6-8 months solo engineer |

**Resource Requirements:**
- Solo engineer: Node.js + Electron + React + TypeScript
- Key risks: MCP SDK complexity (mitigated by Phase 0), solo scope (mitigated by phased exit criteria)
- If Phase 0 overruns: escalate before committing to Electron architecture

---

## Scope Decisions

| Decision | Effort | Status |
|----------|--------|--------|
| Phase 0: Runtime validation sprint (1-2 weeks before canvas) | S | **ACCEPTED — added by eng review** |
| Phase 0 Definition of Done: Run `npm test`, confirm: (1) agent process starts via UtilityProcess.fork(), (2) stdout appears in xterm.js within 2s, (3) spawn/run/error events appear in SQLite within 1s | — | **ACCEPTED** |
| Phase 0 Go/No-Go: If MCP SDK (filesystem server via stdio) validates in Week 1 → proceed to Phase 1. If MCP fails → try custom JSON-RPC stdio framing in Week 1.5. If both fail by end of Week 2 → escalate: reconsider Electron architecture before committing to Phase 1 | — | **ACCEPTED** |
| Phase 1: Canvas MVP — multiple view modes (Kanban, node workflow, adaptive) | M | **ACCEPTED** — prototype ≥2 view modes, test with users to find right fit |
| Phase 2: Full runtime (UtilityProcess + MCP + SQLite) | L | ACCEPTED |
| Phase 3: Orchestrator (policy engine + watch-and-learn) | XL | ACCEPTED — scope: event ingestion, pattern observation, manual policy suggestions. NO automatic enforcement, NO policy rollback, NO multi-agent transactions |
| Electron signing (sideloading v0.1, Mac App Store v1.0) | S | ACCEPTED |
| Claude Code plugin as v0.1 distribution channel | M | **ACCEPTED — Phase 1** |
| Freemium: Solo free, Pro $20/seat/month | S | **ACCEPTED** — Solo free, Pro (1-5 seats) $20/seat |
| Team workspaces | XL | DEFERRED to v2 |

---

## What Survived the Review

1. **The three pillars are the right frame.** Canvas + Runtime + Orchestrator is not incremental — it's a new category claim. The claim is credible because no one else has all three.

2. **"Watch and learn" solves the right problem.** Beginners don't know what policies to configure. The solution (observe first, suggest second) is correct. The risk is that "watching" for 24-72 hours before getting value is too slow for users who need help now.

3. **Electron is the right technical anchor.** Desktop-first avoids cloud complexity, enables full process control, and the dev tools audience accepts installed apps. The risk is Mac App Store distribution (notarization, signing).

4. **The kill condition is right in spirit.** If indie devs don't place ≥3 agents on the canvas in 30 minutes, the UX is wrong. But it should measure task completion, not just canvas interaction.

---

## What Didn't Survive the Review

1. **Revenue model is completely absent.** — **RESOLVED** — Freemium: Solo free, Team $20/seat/month.

2. **"VS Code for agents" is a $100M positioning problem.** — **RESOLVED** — Multi-agent canvas is the core positioning.

3. **The 18-24 month timeline for v1.0 is optimistic.** This assumes all integrations are frictionless. Phase 0 runtime sprint tests this assumption.

4. **The Orchestrator's "watch and learn" has no false-positive budget.** — **RESOLVED** — T7 added: specced before Phase 3.

---

## Competitive Landscape

**Cling Kanban** (announced 2026-03) is the most direct competitor — same scope as AgentOS: Canvas + Runtime + Orchestrator. Browser-based Kanban board for coding agents using git worktrees. Cling started as a VS Code extension (like Kilo Code) — developer-first, coding-agent niche.

**Assessment:** Cling validates. They are building the same three pillars. The coding-agent space is their home turf. AgentOS's differentiator is the **uninitiated** — the marketing managers, ops leads, and business owners who use ChatGPT but have never heard of multi-agent orchestration. The mission is: let a non-technical person assemble a "Google Ads agent team" visually in minutes, without writing code.

**Views are an open question.** Three viable canvas views exist — Kanban boards (Cling), n8n-style node workflows, and others. Each has different cognitive affordances for different user mental models. The right view may differ by persona or use case. AgentOS should support multiple view modes and let user testing reveal which sticks. The secret sauce is not which view wins — it's the Runtime + Orchestrator underneath that makes any view come alive.

**AgentOS's structural advantages vs Cling:**
- Native Electron: full process control, no browser sandbox limitations
- MCP-native: connects to any MCP server (business-relevant tools, not just code)
- Persistent agents: memory and state survive sessions (Cling is session-based)
- Multi-view canvas: adaptive to user mental model, not forced into one paradigm
- Desktop-first: no cloud dependency, works offline, full local data control

The CEO review of the AgentOS Electron design is complete.

**3 blocking decisions resolved:**
1. Revenue model: Freemium Solo free / Pro $20/seat/month (Pro tier until team features ship in v2)
2. v0.1 success metric: Task completion (end-to-end agent run without manual intervention)
3. Persona: Non-technical power users (marketing managers, ops leads) as primary; indie hackers secondary

**Mission Statement (locked in):** Most people will never write a line of code. But every marketing manager, ops lead, and small business owner already knows what it feels like to wish you had more hands. AgentOS gives them a canvas — not a command line — where they can build a team of AI agents, watch those agents work, and trust that someone is keeping an eye on what goes wrong. We are not building another developer tool. We are building the tool that makes multi-agent AI feel inevitable for the person who manages a team, not the person who builds one. The IDE for everyone else.

**Scope decisions:**
- Expanded: Claude Code plugin added to Phase 1 as distribution channel
- Expanded: Phase 0 runtime validation sprint (2 weeks) before canvas work
- Expanded: Multiple view modes (Kanban, node workflow) — test with users to find right fit
- Deferred: Team workspaces → v2; team pricing tier → v2
- Resolved: T7 added (watch-and-learn false-positive budget specced before Phase 3)

**What was rejected:**
- "VS Code for agents" positioning → replaced with "build your own AI agent team visually"
- Single view mode locked in early → views remain flexible until user testing decides

**Competitive position:** Cling validates. They are building the same three pillars (Canvas + Runtime + Orchestrator) for developers. AgentOS's uncrowded wedge is the non-technical user and the business-process niche. The secret sauce is UI/UX taste + ease-of-use.

**Next concrete step:** Phase 0 runtime sprint — spawn one agent, stream stdout to xterm.js, persist events to SQLite. Go/No-Go by end of Week 2.

---

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 2 | CLEAR | 3 blockers resolved (Freemium model, task completion metric, indie hacker persona); 1 scope expansion accepted (Claude Code plugin) |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | — |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 2 | CLEAR | 3 arch issues resolved; Phase 0 added; 7 TODOs (T1-T7) |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | — |

**UNRESOLVED:** 0 — all 3 CEO blockers resolved
**VERDICT:** CEO + ENG CLEARED — proceed with Phase 0 runtime validation
